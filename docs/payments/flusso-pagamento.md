# 💳 Flusso Pagamento Stripe

## Panoramica

Il sistema utilizza Stripe Checkout per gestire i pagamenti in modo sicuro. I fondi vengono trattenuti in escrow fino al completamento del servizio.

## Flusso Completo

```
┌─────────────────────────────────────────────────────────────────┐
│                    FLUSSO PAGAMENTO STRIPE                       │
└─────────────────────────────────────────────────────────────────┘

╔═══════════════════════════════════════════════════════════════╗
║  FASE 1: CREAZIONE CHECKOUT SESSION                           ║
╚═══════════════════════════════════════════════════════════════╝
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  1. FRONTEND: Utente clicca "Conferma e Paga"                   │
│     SmartBookingForm → onSubmit()                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. API CALL: POST /api/bookings                                │
│     Body: {                                                     │
│       serviceId, date, startTime, endTime,                      │
│       squareMetersRange, windowsCount,                          │
│       notes, address, clientPhone                               │
│     }                                                           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. BACKEND: Validazioni                                        │
│     - Servizio esiste?                                          │
│     - Slot disponibile? (double check)                          │
│     - Utente autenticato?                                       │
│     - Prezzo >= €0.50?                                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  4. CALCOLO PREZZO FINALE                                       │
│     price = service.price × (estimatedDuration / 60)           │
│     Esempio: €15/h × 3.5h = €52.50                             │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  5. CREAZIONE STRIPE CHECKOUT SESSION                           │
│                                                                 │
│     const session = await stripe.checkout.sessions.create({    │
│       payment_method_types: ["card"],                          │
│       mode: "payment",                                         │
│       line_items: [{                                           │
│         price_data: {                                          │
│           currency: "eur",                                     │
│           unit_amount: 5250, // €52.50 in centesimi           │
│           product_data: {                                      │
│             name: "Pulizia Appartamento",                      │
│             description: "27 Nov 2025, 08:00-11:30"           │
│           }                                                    │
│         },                                                     │
│         quantity: 1                                            │
│       }],                                                      │
│       success_url: "https://domain.com/booking-success?...",   │
│       cancel_url: "https://domain.com/services",               │
│       metadata: {                                              │
│         serviceId, clientId, providerId, date, ...             │
│       }                                                        │
│     });                                                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  6. RESPONSE AL FRONTEND                                        │
│     {                                                           │
│       url: "https://checkout.stripe.com/c/pay/cs_xxx"          │
│     }                                                           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  7. REDIRECT A STRIPE CHECKOUT                                  │
│     window.location.href = response.url                        │
└─────────────────────────────────────────────────────────────────┘


╔═══════════════════════════════════════════════════════════════╗
║  FASE 2: PAGAMENTO SU STRIPE                                  ║
╚═══════════════════════════════════════════════════════════════╝
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  8. STRIPE CHECKOUT PAGE                                        │
│     ┌─────────────────────────────────────────────────────┐    │
│     │  🔒 Pagamento Sicuro                                │    │
│     │                                                      │    │
│     │  Pulizia Appartamento        €52.50                 │    │
│     │  27 Nov 2025, 08:00-11:30                           │    │
│     │                                                      │    │
│     │  ┌─────────────────────────────────────────┐        │    │
│     │  │ Numero carta: 4242 4242 4242 4242       │        │    │
│     │  │ Scadenza: 12/26    CVV: 123             │        │    │
│     │  └─────────────────────────────────────────┘        │    │
│     │                                                      │    │
│     │  [         Paga €52.50         ]                    │    │
│     └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┴───────────────┐
              ▼                               ▼
       [Pagamento Failed]              [Pagamento OK]
              │                               │
              ▼                               ▼
┌─────────────────────┐         ┌─────────────────────────────────┐
│ Redirect cancel_url │         │  9. STRIPE WEBHOOK              │
│ → /services         │         │     Event: checkout.session     │
└─────────────────────┘         │            .completed           │
                                └─────────────────────────────────┘
                                              │
                                              ▼

╔═══════════════════════════════════════════════════════════════╗
║  FASE 3: WEBHOOK - CREAZIONE BOOKING                          ║
╚═══════════════════════════════════════════════════════════════╝
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  10. BACKEND: Webhook Handler                                   │
│      POST /api/payment/webhook                                  │
│                                                                 │
│      - Verifica firma Stripe (STRIPE_WEBHOOK_SECRET)           │
│      - Estrae metadata dalla session                           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  11. CREAZIONE BOOKING NEL DATABASE                             │
│                                                                 │
│      await prisma.booking.create({                             │
│        data: {                                                  │
│          serviceId: metadata.serviceId,                        │
│          userId: metadata.clientId,                            │
│          date: new Date(metadata.date),                        │
│          startTime: metadata.startTime,                        │
│          endTime: metadata.endTime,                            │
│          squareMetersRange: metadata.squareMetersRange,        │
│          windowsCount: parseInt(metadata.windowsCount),        │
│          estimatedDuration: parseInt(metadata.estimatedDuration),│
│          totalAmount: session.amount_total / 100,              │
│          status: "pending",                                    │
│          paymentIntentId: session.payment_intent,              │
│          paymentStatus: "paid"                                 │
│        }                                                       │
│      });                                                       │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  12. NOTIFICHE                                                  │
│      - Email conferma al cliente                               │
│      - Notifica push al fornitore                              │
│      - Log transazione                                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  13. REDIRECT SUCCESS                                           │
│      → /booking-success?bookingId=xxx                          │
│                                                                 │
│      ┌─────────────────────────────────────────────────────┐   │
│      │  ✅ Prenotazione Confermata!                        │   │
│      │                                                      │   │
│      │  La tua prenotazione è stata registrata.            │   │
│      │  Riceverai una email di conferma.                   │   │
│      │                                                      │   │
│      │  [  Vai alle tue prenotazioni  ]                    │   │
│      └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## File Coinvolti

| Layer    | File                                        | Funzione             |
| -------- | ------------------------------------------- | -------------------- |
| Frontend | `src-react/components/SmartBookingForm.tsx` | Inizia checkout      |
| Frontend | `src-react/services/bookingService.ts`      | API call             |
| Backend  | `src/routes/bookings.ts`                    | Route POST /bookings |
| Backend  | `src/services/bookingService.ts`            | Business logic       |
| Backend  | `src/routes/payment.ts`                     | Webhook handler      |
| Backend  | `src/services/paymentService.ts`            | Stripe integration   |
| Backend  | `src/config/stripe.ts`                      | Stripe config        |

## Configurazione Stripe

```typescript
// src/config/stripe.ts
import Stripe from "stripe";

export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {
  apiVersion: "2023-10-16",
});

// Webhook secret per verificare le richieste
export const STRIPE_WEBHOOK_SECRET = process.env.STRIPE_WEBHOOK_SECRET!;
```

## Webhook Verification

```typescript
// src/routes/payment.ts
app.post(
  "/api/payment/webhook",
  express.raw({ type: "application/json" }),
  async (req, res) => {
    const sig = req.headers["stripe-signature"]!;

    let event: Stripe.Event;
    try {
      event = stripe.webhooks.constructEvent(
        req.body,
        sig,
        STRIPE_WEBHOOK_SECRET
      );
    } catch (err) {
      return res.status(400).send(`Webhook Error: ${err.message}`);
    }

    if (event.type === "checkout.session.completed") {
      const session = event.data.object as Stripe.Checkout.Session;
      await handleCheckoutCompleted(session);
    }

    res.json({ received: true });
  }
);
```

## Metadata Passati a Stripe

```typescript
metadata: {
  serviceId: service.id,
  clientId: userId,
  clientEmail: userEmail,
  providerId: service.providerId,
  date: date,
  startTime: startTime,
  endTime: endTime,
  squareMetersRange: squareMetersRange,
  windowsCount: windowsCount.toString(),
  estimatedDuration: estimatedDuration.toString(),
  notes: notes,
  address: address,
  clientPhone: clientPhone
}
```

## Variabili d'Ambiente

```env
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_SUCCESS_URL=http://localhost:5173/booking-success
STRIPE_CANCEL_URL=http://localhost:5173/services
```

## Test Mode vs Live Mode

| Ambiente | Secret Key    | Carte Test          |
| -------- | ------------- | ------------------- |
| Test     | `sk_test_xxx` | 4242 4242 4242 4242 |
| Live     | `sk_live_xxx` | Carte reali         |

### Carte di Test

| Numero              | Risultato           |
| ------------------- | ------------------- |
| 4242 4242 4242 4242 | Pagamento OK        |
| 4000 0000 0000 0002 | Carta rifiutata     |
| 4000 0000 0000 9995 | Fondi insufficienti |

## Error Handling

```typescript
try {
  const session = await stripe.checkout.sessions.create({...});
  return { url: session.url };
} catch (error) {
  if (error.type === "StripeCardError") {
    throw new Error("Carta rifiutata");
  }
  if (error.type === "StripeInvalidRequestError") {
    throw new Error("Richiesta non valida");
  }
  throw new Error("Errore durante il pagamento");
}
```
