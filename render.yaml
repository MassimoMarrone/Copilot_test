services:
  # Il Backend + Frontend (Node.js)
  - type: web
    name: domy-app
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        sync: false # Ti chiederà di inserirlo manualmente (URL di Neon)
      - key: DIRECT_URL
        sync: false # Ti chiederà di inserirlo manualmente (URL di Neon)

version: "1"
